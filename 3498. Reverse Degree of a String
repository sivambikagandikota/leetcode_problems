class Solution:
    def reverseDegree(self, s: str) -> int:
        st=0
        for i in range(len(s)):
            a=s[i]
            st+=(i+1)*(ord('z')-ord(a)+1)
            print(ord('z')-ord(a)+1)
        return st   
