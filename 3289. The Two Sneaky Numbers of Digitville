class Solution:
    def getSneakyNumbers(self, nums: List[int]) -> List[int]:
        r=[]
        num=set(nums)
        for i in num:
            if nums.count(i)==2:
                r.append(i)
        return r        
